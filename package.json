{
  "name": "story-hub-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Node.js, Express, MongoDB and ImageKit backend for Story Hub.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check scripts/migrate-frontend.js && node --check scripts/seed.js && node --check scripts/upload-media.js",
    "migrate:frontend": "node scripts/migrate-frontend.js",
    "seed": "node scripts/seed.js",
    "media:upload": "node scripts/upload-media.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "@imagekit/nodejs": "^7.10.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mongodb": "^6.19.0",
    "morgan": "^1.10.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
